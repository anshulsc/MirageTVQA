# requirements.txt

# For the Streamlit Dashboard
streamlit
pandas
plotly

# For the offline evaluation script (run_evaluation.py)
# Note: Lightning AI's PyTorch images often have these pre-installed,
# but it's best practice to list them explicitly.
torch
transformers
accelerate
bitsandbytes
tqdm